interface:
  display_name: "WebMCP Bridge"
  short_description: "Connect approved browser tabs to your local agent"
  default_prompt: "Use $webmcp-bridge-ext to install the native host, configure my local harness and guide me through the extension UI. Verify a harmless tool call before making changes."
